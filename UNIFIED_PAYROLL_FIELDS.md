# Унифицированная схема полей DailyPayrollCalculation

## Текущая структура полей (после рефакторинга)

### 🎯 ОСНОВНЫЕ ПОЛЯ (отображаются в Admin)
```python
base_pay         # BASE PAY - Базовая оплата за регулярные часы
bonus_pay        # BONUS PAY - Все доплаты (сверхурочные + праздничные + шabbat)  
total_gross_pay  # TOTAL GROSS PAY - Итоговая сумма
```

### 📊 ДЕТАЛЬНЫЕ ПОЛЯ (для аналитики)
```python
# Часы
regular_hours         # Обычные часы (до 8.6)
overtime_hours_1      # Сверхурочные 1-2 часа (125%)
overtime_hours_2      # Сверхурочные 2+ часов (150%)

# Платежи (переименованы для ясности связи с основными полями)
base_regular_pay          # Компонент base_pay: оплата обычных часов
bonus_overtime_pay_1      # Компонент bonus_pay: оплата первых 2 сверхурочных часов
bonus_overtime_pay_2      # Компонент bonus_pay: оплата дополнительных сверхурочных часов
```

### 🏛️ СПЕЦИАЛЬНЫЕ ПОЛЯ (Sabbath/Holiday)
```python
bonus_sabbath_overtime_pay_1  # Компонент bonus_pay: первые 2 часа Sabbath сверхурочных (175%)
bonus_sabbath_overtime_pay_2  # Компонент bonus_pay: дополнительные Sabbath сверхурочные (200%)
is_holiday                   # Флаг праздничного дня
is_sabbath                   # Флаг Sabbath дня
```

## 🔄 Логика заполнения полей

### В расчетах используем:
```python
# Основная логика
base_pay_amount = regular_hours × hourly_rate
overtime_pay_amount = (overtime_hours_1 + overtime_hours_2) × hourly_rate
holiday_bonus = (if holiday) daily_hours × hourly_rate × 0.5
sabbath_bonus = (if sabbath) daily_hours × hourly_rate × 0.5

# Заполняем поля
base_pay = base_pay_amount                    # Admin: BASE PAY
bonus_pay = overtime + holiday + sabbath      # Admin: BONUS PAY
total_gross_pay = base_pay + bonus_pay        # Admin: TOTAL GROSS PAY
```

## 📋 Соответствие полей в Admin панели

| Admin поле | Database поле | Описание |
|------------|---------------|----------|
| `BASE PAY` | `base_pay` | Базовая оплата за регулярное время |
| `BONUS PAY` | `bonus_pay` | Все доплаты (сверхурочные, праздничные, sabbath) |
| `TOTAL GROSS PAY` | `total_gross_pay` | Общая сумма к выплате |

## ✅ Преимущества унифицированной схемы

1. **Простота для разработчиков**: Понятные названия полей
2. **Соответствие Admin**: Поля называются как в интерфейсе  
3. **Гибкость расчетов**: Детальные поля для анализа остаются
4. **Совместимость**: Старые поля сохранены для обратной совместимости

## 🎨 Будущие улучшения

В будущем можно:
1. Добавить поля `overtime_pay`, `holiday_pay`, `sabbath_pay` для еще большей детализации
2. Убрать дублирующие legacy поля после полного перехода
3. Добавить вычисляемые свойства в модель для автоматического расчета