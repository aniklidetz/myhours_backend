# Local test environment variables for pre-push hooks
# This file is sourced by scripts/pre-push.sh for local testing
# CI uses its own environment variables defined in workflows

DJANGO_SETTINGS_MODULE=myhours.settings_ci
# Use your macOS user which typically has no password and CREATEDB privileges
DATABASE_URL=postgresql://aniklidetz@127.0.0.1:5432/myhours_test

# Optional: Override other settings for local testing
# DEBUG=True
# ENABLE_BIOMETRIC_MOCK=True